import { Card } from '@/components/ui/Card';

export function InsightPanel() {
  return (
    <Card title="Mock insight">
      <p className="leading-7">
        Your recent entries mention overthinking and low energy. A useful next step might be parking one worry, then doing a short grounding exercise before returning to work.
      </p>
    </Card>
  );
}
