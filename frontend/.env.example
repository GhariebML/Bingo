# Production Vercel deployments cannot call localhost.
# Set this to the public Render backend URL, then redeploy Vercel after changing it.
NEXT_PUBLIC_API_BASE_URL=https://bingo-backend-8dub.onrender.com

# Keep the deployed app in safe demo mode.
NEXT_PUBLIC_APP_MODE=demo
